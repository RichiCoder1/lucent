style WidgetStyle {
    Spacing: DensitySpacing;
}

internal component Widget(string value = "lui sdk consumer") {
    <Row style={WidgetStyle}>
        <Text>{value}</Text>
        <Text content={value} />
    </Row>
}
