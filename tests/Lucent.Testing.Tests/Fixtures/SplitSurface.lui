namespace HeadlessFixtures;

using Lucent.Core;

public component SplitSurface(SplitPaneState state) {
    <SplitPane state={state} first={[SplitFirst()]} second={[SplitSecond()]} label="Resize primary pane" />
}
