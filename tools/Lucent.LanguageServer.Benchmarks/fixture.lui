namespace Benchmark;
using Avalonia.Controls;
component App() {
    private readonly State<string> title = new("Lucent");
    Fragment Render() {
        return Border {
            Child: TextBlock { Text: title.Value; }
        };
    }
}
