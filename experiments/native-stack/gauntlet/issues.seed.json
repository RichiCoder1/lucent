{
  "count": 10000,
  "seed": 8128,
  "formula": {
    "id": "issue-{index:D5}",
    "title": "index % 5 == 0 ? Authentication failure {index} : Issue {index}",
    "status": "index % 3 == 0 ? Closed : Open",
    "priority": "index % 4 == 0 ? High : index % 4 == 1 ? Medium : Low",
    "assignee": "index % 2 == 0 ? Ada : Lin"
  },
  "asyncDelayMilliseconds": 50,
  "failureQuery": "fail"
}
