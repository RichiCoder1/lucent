# Issue #12 evidence

`run-issue-12-proof.ps1` regenerates both deterministic walkthroughs, semantic dumps, identities, and matching 1200×760 light/dark PNG captures. It verifies all twelve JSONL records and PNG signatures/dimensions without scoring the frozen rubric.
