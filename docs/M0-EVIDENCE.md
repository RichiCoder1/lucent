# M0 executable evidence

Run `pwsh tools/Verify-M0.ps1` on Windows. It performs locked restore, warning-clean build and `win-x64` NativeAOT publish, exact asset/notice reconciliation (including a removed-asset negative), Core architecture positives and injected negatives, then three ordinary-close lifecycle smokes from a clean copied publish directory.

The static Issue Browser requests SDL high-pixel-density output, queries its actual renderer backing pixels, and establishes an explicit SDL 1.25× logical test presentation mode. The external smoke samples the visible bootstrap background/accent pixels and asserts its 640×400 logical / 800×500 backing contract before ordinary close. Its only paint is the temporary platform-owned bootstrap rectangle; M0 does not claim a framework canvas, kernel, accessibility, input, installer, or signing surface.
