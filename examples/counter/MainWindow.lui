namespace Lucent.Examples.Counter;

using Lucent.Examples.Counter;

component MainWindow()
{
    Fragment Render()
    {
        return Window {
            Title: "Lucent Counter";
            Width: 420;
            Height: 260;
            MinWidth: 420;
            MinHeight: 300;
            WindowStartupLocation: Avalonia.Controls.WindowStartupLocation.CenterScreen;

            Counter {}
        };
    }
}
