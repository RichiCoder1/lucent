# Lucent Workbench

Static multi-file dogfood shell for component inputs, optional slots, conditional composition, and retained child state.

```powershell
dotnet run --project examples/workbench/Lucent.Workbench.csproj -- --smoke-test
```
