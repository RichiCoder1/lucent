namespace Lucent.Examples.Workbench;

using System.Windows.Input;
using Avalonia.Automation;

component ProblemsPane(ICommand copyCommand, int count = 2) => Border {
    StackPanel {
        TextBlock { Text: $"{count} problems"; AutomationProperties.Name: "Problems"; }
        Button { Content: "Copy diagnostic"; Command: copyCommand; }
    }
};
