namespace Lucent.ComponentBrowser;

public component ComponentBrowserApplication() {
    context ThemeContext theme;

    <Router
        routes={ComponentBrowserRoutes.Bundle}
        initial="/examples/buttons"
        name="component-browser.router"
    >
        <ComponentBrowserHost theme={theme} name="Component Browser" />
    </Router>
}
